# PPO-from-Scratch-in-Unity-3D-Target-Collector (Python-Low-Level-API)
Proximal Policy Optimization (PPO) is not only applicable to continuous action spaces by sampling actions from probability distributions, but also clips the probability ratio between the new and old policies to prevent large policy updates.
